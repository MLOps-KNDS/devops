apiVersion: v1
kind: Service
metadata:
  name:  {{ .Values.api.name }}
spec:
  selector:
    app:  {{ .Values.db.name }}
  ports:
    - protocol: {{ .Values.db.service.protocol }}
      port: {{ .Values.db.service.port }}
      targetPort: {{ .Values.db.service.target }}