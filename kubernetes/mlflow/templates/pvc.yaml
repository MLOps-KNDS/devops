apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlflow-storage
  namespace: app
spec:
  storageClassName: mlflow-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
